[build]
  command = ""
  publish = "."
  functions = "netlify/functions"

# Embarque les fichiers du dossier data/ avec les Functions (sinon admin-build-index
# ne trouve pas data/livre.md à l'exécution).
[functions]
  included_files = ["data/**"]

# Fonction planifiée : rappel push ~30 min avant chaque séance.
[functions."session-reminders"]
  schedule = "*/10 * * * *"

[[headers]]
  for = "/sw.js"
  [headers.values]
    Cache-Control = "no-cache"
    Service-Worker-Allowed = "/"

[[headers]]
  for = "/firebase-messaging-sw.js"
  [headers.values]
    Cache-Control = "no-cache"
    Service-Worker-Allowed = "/"

[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "SAMEORIGIN"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
